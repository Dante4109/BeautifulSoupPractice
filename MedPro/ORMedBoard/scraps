print(
                licenses,
                request.url,
                request.response.status_code,
                request.response.headers['Content-Type']
            )
    for license in licenses:
        print(license)
        print("-------------")
